;; ctags設定
;; M-x anything-ctags-current-file
;; http://ctags.sourceforge.net/
;; $ ctags -R -e
;; $ ctags -e *.rb
;; $ find . -name '*.rb' | xargs ctags -e
;; $ find /app -name '*.rb' | xargs ctags -e
;; M-x anything-c-etags-select => C-x c e
;; GNU/Linuxでは
;; grep -nH -e foo /tmp
